import { EventEmitter } from "eventemitter3";

export const eventLeaderboard = new EventEmitter();

export const GAME_WIDTH = window.innerWidth;
export const GAME_HEIGHT = window.innerHeight;
export const TIME_OUT = 60
